# Pickleball project rules

Read and follow `AGENTS.md` before handling a task.

For functionality changes, read `docs/agent/feature-map.md`, inspect all related framework code, tests, Maven consumer scenarios, test-site resources, configuration, and documentation, then update every affected surface.

Use the build, compatibility, documentation, and definition-of-done requirements in `AGENTS.md`. Do not ask the user to repeat project background already stored in the repository.
