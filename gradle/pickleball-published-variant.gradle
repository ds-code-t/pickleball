// Retained as a migration tombstone for drop-in upgrades. Workbench no longer
// resolves any published-equivalent/root runtime variant. Its only shared Java
// dependency is :pickleball-control-protocol; the completed controller JAR is
// embedded outward into the Pickleball distribution as opaque bytes.
