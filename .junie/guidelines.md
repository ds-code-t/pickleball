# Pickleball Junie guidelines

Read and follow `/AGENTS.md` before working in this repository.

For functionality changes, also read `/docs/agent/feature-map.md`, inspect the relevant implementation, tests, Maven consumer scenarios, local test-site support, configuration, and documentation, then update every affected surface and run the validation defined in `AGENTS.md`.

Do not ask the user to repeat project setup already recorded in the repository.
