---
applyTo: "maven-consumer-project/**"
---

Follow `/AGENTS.md`.

Treat `maven-consumer-project` as both an example consumer and executable compatibility suite. Keep feature scenarios, call definitions, configuration, local server endpoints/pages, runner support, and framework behavior synchronized. Do not simplify an example in a way that stops testing the public behavior it documents.
