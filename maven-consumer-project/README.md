# Pickleball Maven Consumer Example

This is a standalone Maven example project that consumes Pickleball as a test-scoped dependency and exercises browser and service behavior against its local test site.

## Run

```bash
mvn test
```

Or use the included Maven wrapper.
