package tools.dscode.common.control;

// Intentionally empty. The annotation-style control aspect is compiled from
// src/main/aspectj/tools/dscode/common/control/ControlRuntimeAspect.aj so ajc
// sees it while Gradle Javadoc does not require AspectJ runtime types.
